const state = {
  loading: false,
};

const getters = {};

const actions = {};

const mutations = {
  setLoading(state, loading) {
    state.loading = loading;
  },
};

export default {
  state,
  getters,
  actions,
  mutations,
  namespaced: true,
};
